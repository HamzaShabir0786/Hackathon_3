export const metadata = {
  title: "contact",
  description: "Contact Us!",
  openGraph: {
    title: "Contact Page",
    description: "Discover and Contact Us !",
  },
};

export default function ShopLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <> {children}</>;
}
